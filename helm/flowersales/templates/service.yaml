apiVersion: v1
kind: Service
metadata:
  name: {{ include "flowersales.fullname" . }}
  labels:
    {{- include "flowersales.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flowersales.selectorLabels" . | nindent 4 }}
